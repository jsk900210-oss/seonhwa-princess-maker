# 버전 저장 규칙

- 모든 수정은 먼저 디버깅 버전으로 저장한다 (`v0.61.9-debug`).
- 기능 추가: 부 버전 증가 (`0.4.0` → `0.5.0`)
- 작은 화면 수정과 버그 수정: 패치 버전 증가 (`0.4.0` → `0.4.1`)
- 각 업로드 전 `README.md`, `CHANGELOG.md`, `UPDATE_ATTEMPTS.md`를 갱신한다.
- 커밋 제목 형식: `v버전: 변경 요약`
- 디버깅 태그 형식: `v0.61.9-debug`
- 안정 확인 시 Git 태그 형식: `v0.61.9`
- 기본 브랜치에는 실행 확인된 버전만 저장한다.
- 실험적 변경은 `debug/기능명` 브랜치에 먼저 저장한다.
- 푸시 명령은 브랜치와 태그를 모두 포함한다: `git push origin <브랜치> --follow-tags`.
- GitHub 인증 또는 네트워크 문제로 푸시하지 못한 경우 README에 `로컬 커밋 완료 / 원격 미반영`이라고 기록한다.
