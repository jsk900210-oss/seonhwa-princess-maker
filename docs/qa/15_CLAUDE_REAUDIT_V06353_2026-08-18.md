# 《선화공주 만들기》 Claude 보고서 재검수 — v0.63.53-debug

검수일: 2026-08-18  
기준: Claude의 `v0.63.50-debug / d9561aa` 교차검수 보고서와 현재 코드 직접 대조

## 수정 완료

- `schedule.css` 맨 아래의 `v0.63.50`·`v0.63.51` 최종 계약을 `v0.63.53` 단일 계약으로 통합했습니다.
- 최신 사용자 확정값인 큰 달력(일반 132×104, 380px 이하 124×98)을 공식 계약으로 고정했습니다.
- 휴일 쯔꾸르 프레임의 외부 타원형 그림자를 제거하고 내부 테두리만 유지했습니다.
- 페이즈 종료 결산의 수입 줄에 승급이 발생한 경우 `숙련/달인 승급!`을 함께 표시합니다.
- 상태 화면 인연 안내를 `5회 만남 → 데이트`, `호감도 60 이상 + 특별한 관계 → 엔딩 후보`로 명확히 표시합니다.
- 리팩터 이전 문자열을 찾던 자동검사 12개를 현재 구조와 실제 기능을 검사하도록 갱신했습니다.

## Claude 보고서 항목 재판정

- HIGH #1 최종 계약 충돌: 해결. 최신 요구를 반영한 `v0.63.53` 계약 하나만 존재합니다.
- MEDIUM-HIGH #2 중복 누적: 최종 승자 블록을 하나로 통합했습니다. 과거 실험 규칙은 위쪽에 남아 있으나 최종 계약은 단일 블록입니다.
- MEDIUM #3 승급 표시 누락: 해결.
- LOW-MEDIUM #4 인연 조건 안내: 최신 코드에 이미 존재했으며 문구를 더 명확히 보강했습니다.
- 모바일 #5 달력 정렬: 360/390/430px 실측 완료. 달력과 캐시 카드 상단선이 일치하며 서로 겹치지 않습니다.
- 모바일 #6 휴일 외부 반원/그림자: 360px 휴일 실화면에서 제거 확인.

## 자동검사 결과

- Node 기능 검사: `28/28 PASS`
- JavaScript 구문 검사: PASS
- 프로젝트 구조 및 정적 자산 참조: READY
- 핵심 자산 규격 검사: PASS
- 전체 래스터 자산 검사: 754개, 오류 0, 경고 0

## 모바일 실측

- 360px: 휴일 장면 확인, 외부 그림자 없음, 달력/캐시 상단 약 33px로 정렬
- 390px: 달력 132×104, 캐시 카드와 비겹침
- 430px: 달력 132×104, 스테이터스와 대화창 비겹침
- 브라우저 콘솔 오류/경고: 0건

## 참고

전체 자산 감사에서 동일 파일 해시 1쌍이 발견됐습니다.
`wardrobe/age-16/age13-scholar.png`와 `wardrobe/age-18/age13-scholar.png`이며, 감사 도구 판정은 오류·경고 0건입니다. 현재 기능 오류로 분류하지 않습니다.
